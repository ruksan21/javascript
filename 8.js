// shift can help to remove the first element of an array
const arr = [1, 2, 3, 4, 5];
arr.shift();
console.log(arr);


// unshift can help to add an element at the beginning of an array
const arr1 = [1, 2, 3, 4, 5];
arr1.unshift(0);
console.log(arr1);
